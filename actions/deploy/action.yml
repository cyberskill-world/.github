---
name: 🚀 Deploy
description: This action deploys the project to a server using SSH and restarts the PM2 app.
inputs:
  HOST:
    description: The host of the server to deploy to
    required: true
  PORT:
    description: The port to connect to the server
    required: true
    default: 22
  USERNAME:
    description: The username to connect to the server
    required: true
  PASSWORD:
    description: The password to connect to the server
    required: false
  KEY:
    description: The private key to connect to the server
    required: true
  PASSPHRASE:
    description: The passphrase for the private key
    required: false
  PATH:
    description: The path to the project on the server
    required: true
  BRANCH:
    description: The branch to deploy from
    required: true
runs:
  using: "composite"
  steps:
    - name: Deploy to server
      uses: appleboy/ssh-action@0ff4204d59e8e51228ff73bce53f80d53301dee2  # v1.2.5
      env:
        DEPLOY_PATH: ${{ inputs.PATH }}
        DEPLOY_BRANCH: ${{ inputs.BRANCH }}
      with:
        host: ${{ inputs.HOST }}
        port: ${{ inputs.PORT }}
        username: ${{ inputs.USERNAME }}
        password: ${{ inputs.PASSWORD }}
        key: ${{ inputs.KEY }}
        passphrase: ${{ inputs.PASSPHRASE }}
        envs: DEPLOY_PATH,DEPLOY_BRANCH
        script: |
          set -e
          if [ -f ~/.profile ]; then source ~/.profile; fi
          if [ -f ~/.nvm/nvm.sh ]; then source ~/.nvm/nvm.sh; fi || true

          echo "🚀 Deploying to $DEPLOY_PATH..."
          cd "$DEPLOY_PATH"

          # Validate DEPLOY_BRANCH to prevent git option/refspec injection
          if [ -z "$DEPLOY_BRANCH" ]; then
            echo "Error: DEPLOY_BRANCH is empty." >&2
            exit 1
          fi

          case "$DEPLOY_BRANCH" in
            -*)
              echo "Error: DEPLOY_BRANCH must not start with '-'." >&2
              exit 1
              ;;
            *:*)
              echo "Error: DEPLOY_BRANCH must not contain ':'." >&2
              exit 1
              ;;
            refs/*)
              echo "Error: DEPLOY_BRANCH must not start with 'refs/'." >&2
              exit 1
              ;;
          esac

          if ! git check-ref-format --branch "$DEPLOY_BRANCH"; then
            echo "Error: DEPLOY_BRANCH '$DEPLOY_BRANCH' is not a valid branch name." >&2
            exit 1
          fi
          echo "⬇️ Fetching latest changes..."
          git fetch origin +refs/heads/"$DEPLOY_BRANCH":refs/remotes/origin/"$DEPLOY_BRANCH"
          git checkout "$DEPLOY_BRANCH"
          git pull origin "$DEPLOY_BRANCH"

          echo "📦 Installing dependencies..."
          pnpm install --frozen-lockfile

          echo "🏗️ Building project..."
          pnpm build

          echo "🔄 Reloading application..."
          pm2 startOrReload ecosystem.config.cjs --update-env
          pm2 save

          echo "✅ Deployment successful!"
