---
name: 🚀 Deploy
description: This action deploys the project to a server using SSH and restarts the PM2 app.
inputs:
  HOST:
    description: The host of the server to deploy to
    required: true
  PORT:
    description: The port to connect to the server
    required: true
    default: "22"
  USERNAME:
    description: The username to connect to the server
    required: true

  KEY:
    description: The private key to connect to the server
    required: true
  PASSPHRASE:
    description: The passphrase for the private key
    required: false
  PATH:
    description: The path to the project on the server
    required: true
  BRANCH:
    description: The branch to deploy from
    required: true
  APP_NAME:
    description: PM2 app name for targeted health check (if empty, checks any online process)
    required: false
  HEALTH_CHECK_RETRIES:
    description: Number of health check retry attempts
    required: false
    default: "5"
  HEALTH_CHECK_INTERVAL:
    description: Seconds to wait between health check retries
    required: false
    default: "3"
  BUILD_COMMAND:
    description: "Command to install deps and build the project (runs on remote server)"
    required: false
    default: "pnpm install --frozen-lockfile && pnpm build"
  RELOAD_COMMAND:
    description: "Command to reload the application after build (runs on remote server)"
    required: false
    default: "pm2 startOrReload ecosystem.config.cjs --update-env && pm2 save"
runs:
  using: "composite"
  steps:
    - name: Deploy to server
      uses: appleboy/ssh-action@0ff4204d59e8e51228ff73bce53f80d53301dee2  # v1.2.5
      env:
        DEPLOY_PATH: ${{ inputs.PATH }}
        DEPLOY_BRANCH: ${{ inputs.BRANCH }}
        DEPLOY_APP_NAME: ${{ inputs.APP_NAME }}
        HEALTH_RETRIES: ${{ inputs.HEALTH_CHECK_RETRIES }}
        HEALTH_INTERVAL: ${{ inputs.HEALTH_CHECK_INTERVAL }}
        BUILD_CMD: ${{ inputs.BUILD_COMMAND }}
        RELOAD_CMD: ${{ inputs.RELOAD_COMMAND }}
      with:
        host: ${{ inputs.HOST }}
        port: ${{ inputs.PORT }}
        username: ${{ inputs.USERNAME }}
        key: ${{ inputs.KEY }}
        passphrase: ${{ inputs.PASSPHRASE }}
        envs: DEPLOY_PATH,DEPLOY_BRANCH,DEPLOY_APP_NAME,HEALTH_RETRIES,HEALTH_INTERVAL,BUILD_CMD,RELOAD_CMD
        script: |
          set -euo pipefail
          if [ -f ~/.profile ]; then source ~/.profile; fi
          if [ -f ~/.nvm/nvm.sh ]; then source ~/.nvm/nvm.sh; fi || true

          # --- Validation ---
          # ⚠️ SYNC: This function is duplicated from scripts/validate-branch.sh
          # because this script runs via SSH on the remote server where the file isn't available.
          # Keep both copies in sync!
          validate_branch() {
            local name="$1"
            local value="$2"

            if [[ -z "$value" ]]; then
              echo "❌ Invalid branch name for $name: must not be empty" >&2
              exit 1
            fi

            if [[ "$value" == -* ]]; then
              echo "❌ Invalid branch name for $name: must not start with '-'" >&2
              exit 1
            fi

            if [[ "$value" == *:* ]]; then
              echo "❌ Invalid branch name for $name: must not contain ':' (refspecs are not allowed)" >&2
              exit 1
            fi

            if [[ "$value" == refs/* ]]; then
              echo "❌ Invalid branch name for $name: must not start with 'refs/'" >&2
              exit 1
            fi

            if ! git check-ref-format --branch "$value" > /dev/null 2>&1; then
              echo "❌ Invalid branch name for $name: '$value' is not a valid Git branch" >&2
              exit 1
            fi
          }

          if ! command -v git >/dev/null 2>&1; then
            echo "❌ Error: git is not installed or not available in PATH." >&2
            exit 1
          fi

          validate_branch "DEPLOY_BRANCH" "$DEPLOY_BRANCH"

          if [[ -z "$DEPLOY_PATH" ]]; then
            echo "❌ DEPLOY_PATH must not be empty" >&2; exit 1
          fi
          if [[ "$DEPLOY_PATH" == *..* ]]; then
            echo "❌ DEPLOY_PATH must not contain '..' (path traversal)" >&2; exit 1
          fi
          # Resolve relative paths to absolute (relative to $HOME)
          if [[ "$DEPLOY_PATH" != /* ]]; then
            DEPLOY_PATH="$HOME/$DEPLOY_PATH"
            echo "ℹ️ Resolved relative path to: $DEPLOY_PATH"
          fi
          if [[ ! -d "$DEPLOY_PATH" ]]; then
            echo "❌ DEPLOY_PATH '$DEPLOY_PATH' does not exist or is not a directory" >&2; exit 1
          fi

          echo "🚀 Deploying to $DEPLOY_PATH..."
          cd "$DEPLOY_PATH"

          # Capture current commit for rollback
          PREV_COMMIT=$(git rev-parse HEAD)

          echo "⬇️ Fetching latest changes..."
          # ⚡ Bolt: Targeted fetch for performance, with fallback for non-standard refs/tags
          git fetch origin +refs/heads/"$DEPLOY_BRANCH":refs/remotes/origin/"$DEPLOY_BRANCH" || git fetch origin "$DEPLOY_BRANCH"

          # ⚡ Bolt: Avoid 'git pull' which fails on detached HEAD (tags).
          # Checkout local branch if it exists, otherwise detached FETCH_HEAD
          git checkout -q "$DEPLOY_BRANCH" 2>/dev/null || git checkout -q FETCH_HEAD

          # Reset hard to ensure we match the remote state exactly
          git reset --hard FETCH_HEAD

          echo "📦 Building project..."
          bash -c "$BUILD_CMD"

          echo "🔄 Reloading application..."
          bash -c "$RELOAD_CMD"

          # Health check with retry loop and rollback on failure
          APP_NAME_FILTER="${DEPLOY_APP_NAME:-}"
          HEALTH_OK=0
          for attempt in $(seq 1 "$HEALTH_RETRIES"); do
            echo "⏳ Health check attempt $attempt/$HEALTH_RETRIES..."
            sleep "$HEALTH_INTERVAL"
            if [ -n "$APP_NAME_FILTER" ]; then
              HEALTH_OK=$(pm2 jlist | jq -r --arg name "$APP_NAME_FILTER" '.[] | select(.name == $name) | .pm2_env.status' | grep -c "online" || true)
            else
              HEALTH_OK=$(pm2 ls | grep -c "online" || true)
            fi
            if [ "$HEALTH_OK" -gt 0 ]; then
              break
            fi
          done

          if [ "$HEALTH_OK" -eq 0 ]; then
            echo "❌ Health check failed! Rolling back to $PREV_COMMIT..."
            git reset --hard "$PREV_COMMIT"
            bash -c "$BUILD_CMD"
            bash -c "$RELOAD_CMD"

            # Verify rollback succeeded with same health check
            ROLLBACK_OK=0
            for attempt in $(seq 1 "$HEALTH_RETRIES"); do
              echo "⏳ Rollback health check attempt $attempt/$HEALTH_RETRIES..."
              sleep "$HEALTH_INTERVAL"
              if [ -n "$APP_NAME_FILTER" ]; then
                ROLLBACK_OK=$(pm2 jlist | jq -r --arg name "$APP_NAME_FILTER" '.[] | select(.name == $name) | .pm2_env.status' | grep -c "online" || true)
              else
                ROLLBACK_OK=$(pm2 ls | grep -c "online" || true)
              fi
              if [ "$ROLLBACK_OK" -gt 0 ]; then
                break
              fi
            done

            if [ "$ROLLBACK_OK" -eq 0 ]; then
              echo "🚨 CRITICAL: Rollback to $PREV_COMMIT also failed! Manual intervention required."
              exit 2
            fi

            echo "⚠️ Rolled back to $PREV_COMMIT"
            exit 1
          fi

          echo "✅ Deployment successful!"
