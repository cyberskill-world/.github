name: 🌍 CS - Distribute
on:
  workflow_call:
jobs:
  distribute:
    runs-on: ubuntu-latest
    steps:
      # Templates
      - name: Template Frontend Vite React
        uses: cyberskill-world/.github/actions/trigger-events@main
        with:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          REPO: "cyberskill-world/template-fe-vite-react"
          EVENTS: |
            📈 CI - Update

      - name: Template Frontend NextJS
        uses: cyberskill-world/.github/actions/trigger-events@main
        with:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          REPO: "cyberskill-world/template-fe-nextjs"
          EVENTS: |
            📈 CI - Update

      - name: Template Backend NodeJS
        uses: cyberskill-world/.github/actions/trigger-events@main
        with:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          REPO: "cyberskill-world/template-be-nodejs"
          EVENTS: |
            📈 CI - Update

      - name: Template Backend NestJS
        uses: cyberskill-world/.github/actions/trigger-events@main
        with:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          REPO: "cyberskill-world/template-be-nestjs"
          EVENTS: |
            📈 CI - Update

      - name: Template Mobile React Native
        uses: cyberskill-world/.github/actions/trigger-events@main
        with:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          REPO: "cyberskill-world/template-mobile-react-native"
          EVENTS: |
            📈 CI - Update

      - name: Template Mobile Flutter
        uses: cyberskill-world/.github/actions/trigger-events@main
        with:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          REPO: "cyberskill-world/template-mobile-flutter"
          EVENTS: |
            📈 CI - Update
      # Internal products
      - name: Cyber Editor
        uses: cyberskill-world/.github/actions/trigger-events@main
        with:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          REPO: "cyberskill-world/cyber-editor"
          EVENTS: |
            📈 CI - Update

      - name: Cyber Sheet
        uses: cyberskill-world/.github/actions/trigger-events@main
        with:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          REPO: "cyberskill-world/cyber-sheet"
          EVENTS: |
            📈 CI - Update

      - name: Landing Page
        uses: cyberskill-world/.github/actions/trigger-events@main
        with:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          REPO: "cyberskill-world/landing-page"
          EVENTS: |
            📈 CI - Update
      # CNLGaming
      - name: CNLGaming Backend
        uses: cyberskill-world/.github/actions/trigger-events@main
        with:
          GH_TOKEN: ${{ secrets.GH_TOKEN_CNL }}
          REPO: "Cnlgaming-com/cnlgaming-be"
          EVENTS: |
            📈 CI - Update

      - name: CNLGaming Frontend
        uses: cyberskill-world/.github/actions/trigger-events@main
        with:
          GH_TOKEN: ${{ secrets.GH_TOKEN_CNL }}
          REPO: "Cnlgaming-com/cnlgaming-fe"
          EVENTS: |
            📈 CI - Update
      # ODHQ
      - name: ODHQ Backend
        uses: cyberskill-world/.github/actions/trigger-events@main
        with:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          REPO: "cyberskill-world/odhq-be"
          EVENTS: |
            📈 CI - Update

      - name: ODHQ Admin
        uses: cyberskill-world/.github/actions/trigger-events@main
        with:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          REPO: "cyberskill-world/odhq-admin"
          EVENTS: |
            📈 CI - Update
